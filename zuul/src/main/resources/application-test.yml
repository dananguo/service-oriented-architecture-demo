server:
  port: 8081
  address: 0.0.0.0
spring:
  profiles:
    active: test
  application:
    name: gateway-service-zuul
eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-node2:9000/eureka/

