spring:
  profiles:
    active: pro
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: spring-cloud-config-server
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9000/eureka/