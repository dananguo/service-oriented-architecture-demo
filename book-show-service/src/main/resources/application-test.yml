server:
  port: 8085
  address: 0.0.0.0
spring:
  profiles:
    active: test
  application:
    name: book-show-service
eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-node2:9000/eureka/,http://eureka-node1:9000/eureka/
feign:
  hystrix:
    enabled: true
management:
  endpoints:
    web:
      exposure:
        include: "*"
