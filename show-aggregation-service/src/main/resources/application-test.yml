server:
  port: 8089
  address: 0.0.0.0
spring:
  profiles:
    active: test
  application:
    name: show-aggregation-service
eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-node2:9000/eureka/
feign:
  hystrix:
    enabled: true
management:
  endpoints:
    web:
      exposure:
        include: "*"
