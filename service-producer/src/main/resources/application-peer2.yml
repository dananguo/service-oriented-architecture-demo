server:
  port: 8086
spring:
  profiles:
    active: peer1
  application:
    name: spring-cloud-producer
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9000/eureka/ # 需要改动
management:
  endpoints:
    web:
      exposure:
        include: "*"