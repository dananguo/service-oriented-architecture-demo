server:
  port: 8086
spring:
  profiles:
    active: test
  application:
    name: index-show-service
eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka_node2:9000/eureka/,http://eureka_node1:9000/eureka/
feign:
  hystrix:
    enabled: true
management:
  endpoints:
    web:
      exposure:
        include: "*"
