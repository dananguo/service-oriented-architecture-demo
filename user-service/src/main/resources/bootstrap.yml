spring:
  cloud:
    config:
      name: soa-config
      profile: dev
      label: master
      discovery:
        enabled: true
        serviceId: spring-cloud-config-server
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9000/eureka/