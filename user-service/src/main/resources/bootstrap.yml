eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9000/eureka/
spring:
  cloud:
    config:
      name: soa-config
      profile: dev
      label: master
      discovery:
        enabled: true
        serviceId: spring-cloud-config-server
  profiles:
    active: dev